# FastAPI and ASGI Server
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.18
websockets==13.1

# Google Gemini AI
google-generativeai==0.8.3

# HuggingFace Transformers & Audio
transformers==4.46.3
librosa==0.10.2
soundfile==0.12.1
tokenizers==0.15.2

# NOTE: torch and torchaudio are heavy packages and may pull GPU artifacts
# (triton, nvidia packages) or large wheels which can fail on slow networks.
# Make PyTorch optional — install it only if you need the PyTorch fallback
# or plan to convert from a PyTorch model locally. Example (CPU-only):
#   pip install --index-url https://download.pytorch.org/whl/cpu torch==2.5.1+cpu torchaudio==2.5.1+cpu
# Or install PyTorch later with the official instructions:
#   https://pytorch.org/get-started/locally/
# If you will only use ONNX inference, you can leave torch/torchaudio uninstalled.

# ONNX Runtime (for optimized inference)
onnxruntime==1.20.1
onnx==1.17.0

# Data validation
pydantic==2.10.3
pydantic-settings==2.6.1

# HTTP client
httpx==0.28.1
aiohttp==3.11.10
requests==2.32.3
numpy==2.1.3
scipy==1.14.1

# Utilities
python-dotenv==1.0.1
